[project]
name = "custom-image-demo"
version = "0.1.0"
description = "Custom Docker image deployment demo for the MLDLC platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mldlc-sdk",
    "pydantic>=2.13.3",
    "python-dotenv>=1.2.1",
]

[tool.uv.sources]
mldlc-sdk = { path = "../sdk", editable = true }
